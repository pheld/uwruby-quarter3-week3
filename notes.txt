NEED TO

defining:
X keep track of work tasks to be done
X keep track of dependencies for each work task
X provide means of calling the 'work' method from the DSL 

running:
X run the code in the 'recipe' block
X run dependencies before running the current task
X keep track of work tasks that have already been run
- don't run dependencies that have already been run
X run the code block for the specified task
